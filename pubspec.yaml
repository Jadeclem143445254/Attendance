name: svti_attendance_app
description: SVTI Mobile Attendance System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  geolocator: ^11.0.0
  camera: ^0.10.5+9
  permission_handler: ^11.3.0

flutter:
  uses-material-design: true
